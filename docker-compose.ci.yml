version: '3.4'

services:
  app:
    environment:
      DJANGO_SETTINGS_MODULE: ratom.settings.dev
    build:
      context: .
      dockerfile: ./Dockerfile
      target: test-base
    image: govsanc/ratom-server:test-base
